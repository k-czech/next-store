const title = "Next store by NextJS";
const description = "Next store by NextJS";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    siteName: "Next store",
  },
};

export default nextSeoConfig;
